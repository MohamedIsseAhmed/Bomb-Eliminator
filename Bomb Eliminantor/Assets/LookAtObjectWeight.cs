using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LookAtObjectWeight : MonoBehaviour
{
    
    float targetRig;
  
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //rig.weight = Mathf.Lerp(rig.weight, targetRig, 10 * Time.deltaTime);
        //if (Input.GetKeyDown(KeyCode.T))
        //{
        //    targetRig = 1f;
        //}
        //if (Input.GetKeyDown(KeyCode.Y))
        //{
        //    targetRig = 0;
        //}
    }
}
